# Document Manager Example Configuration

# OpenFiles Configuration (Required)
OPENFILES_API_KEY=oa_test123456789012345678901234567890

# OpenAI Configuration (Required for openai-example.ts and tools-example.ts)
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini

# For local development:
# 1. Copy this file to .env
# 2. Update OPENFILES_API_KEY with your actual API key (must start with "oa_")
# 3. Update OPENAI_API_KEY with your actual OpenAI API key
# 4. Ensure OpenFiles API is running locally or update the URL

# Application Configuration
NODE_ENV=development

# SDK Logging Configuration (optional)
# Set log level for OpenFiles SDK operations
# Options: none (default), error, info, debug
# OPENFILES_LOG=info